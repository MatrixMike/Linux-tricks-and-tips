#!/bin/sh

aclocal --version
autoconf --version
autoheader --version
automake --version

glib-gettextize --version
intltoolize --version
libtoolize --version

# see output from autogen in util-linux repo

